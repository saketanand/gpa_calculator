import 'package:flutter/material.dart';

class displaySgpa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Display SGPA Page'),
    );
  }
}
